spring:
  datasource:
    url: jdbc:postgresql://postgres:5433/fairshare
    username: fairuser
    password: fairpass
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties: 
      hibernate:
        format_sql: true
  liquibase:
    enabled: false
server:
  forward-headers-strategy: native




